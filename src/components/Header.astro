---
interface Props {
  sectionTitle?: string;
}

const { sectionTitle } = Astro.props;

const currentPath = Astro.url.pathname;
---

<header class="bg-tech-bg font-mono">
  <nav class="container mx-auto px-4 py-4">
    <div class="flex items-center justify-end">
      <a 
        href="/blog"
        class="px-4 py-2 border-2 rounded transition-all uppercase tracking-wide text-sm font-medium border-tech-border text-text-muted hover:border-accent-secondary hover:text-accent-secondary"
      >
        Blog
      </a>
    </div>
  </nav>
</header>
